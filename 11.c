#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter 2 no:");
    scanf("%d%d",&a,&a);
    if(a==b)
    printf("Numbers are equal\n");
else if(a>b)
printf("%d is greater than %d\n",a,b);
else
printf("%d is less than %d\n",a,b);
return 0;
}